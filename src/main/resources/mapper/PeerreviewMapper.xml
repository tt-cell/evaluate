<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis2.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evaluate.demo.dao.PeerReviewDao">
    <select id="selectAllPeer" parameterType="int" resultType="com.evaluate.demo.entity.User">
        select courses_name,uname from users u,users_teach_courses utc,courses c
where u.uid=utc.uid and utc.courses_id=c.courses_id and u.colleges_id=(select colleges_id from users where uid=#{uid}) and u.uid!=#{uid} group by c.courses_id
LIMIT #{before},#{after}
    </select>
    <select id="countPeer" parameterType="int" resultType="int">
        select courses_name,uname from users u,users_teach_courses utc,courses c
where u.uid=utc.uid and utc.courses_id=c.courses_id and u.colleges_id=(select colleges_id from users where uid=#{uid}) and u.uid!=#{uid} group by c.courses_id
    </select>
</mapper>